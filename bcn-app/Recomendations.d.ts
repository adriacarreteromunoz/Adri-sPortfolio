import * as React from 'react';
export interface RecomendationsProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const Recomendations: React.FC<RecomendationsProps>;
export default Recomendations;
