import * as React from 'react';
export interface FavoritosProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const Favoritos: React.FC<FavoritosProps>;
export default Favoritos;
